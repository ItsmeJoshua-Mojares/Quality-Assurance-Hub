<UserControl x:Class="QAHub.Views.CaparView" x:Name="Root"
             xmlns:ui="clr-namespace:System.Windows.Automation;assembly=PresentationCore"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <UserControl.Resources>
        <Style x:Key="CaparCardStyle" TargetType="Border" BasedOn="{StaticResource StatCardStyle}">
            <Setter Property="Margin" Value="0,0,0,12"/>
        </Style>

        <Style x:Key="ProjectCardStyle" TargetType="Border" BasedOn="{StaticResource StatCardStyle}">
            <Setter Property="Margin" Value="0,0,0,12"/>
        </Style>

        <Style x:Key="FieldLabelStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="#6B7280"/>
            <Setter Property="Margin" Value="0,0,0,4"/>
        </Style>

        <Style x:Key="DetailLabelStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="#6B7280"/>
        </Style>

        <Style x:Key="SecondaryButtonStyle" TargetType="Button" BasedOn="{StaticResource PrimaryButtonStyle}">
            <Setter Property="Background" Value="#F3F4F6"/>
            <Setter Property="Foreground" Value="#111827"/>
        </Style>
    </UserControl.Resources>

    <ScrollViewer x:Name="PageScroller" VerticalScrollBarVisibility="Auto" Padding="26">
        <Grid>

            <!-- ============ PROJECTS DASHBOARD ============ -->
            <StackPanel Visibility="{Binding IsProjectsView, Converter={StaticResource BoolToVisConverter}}">

                <Grid Margin="0,0,0,22">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <StackPanel Grid.Column="0">
                        <TextBlock Text="CAPAR Tracker" FontSize="26" FontWeight="Bold"/>
                        <TextBlock Text="Select a project to see the action requests filed under it"
                                   Foreground="#6B7280" Margin="0,3,0,0"/>
                    </StackPanel>
                    <Button Grid.Column="1" Content="&#8635; Refresh" Style="{StaticResource SecondaryButtonStyle}"
                            Command="{Binding RefreshCommand}" VerticalAlignment="Center" Margin="16,0,0,0"/>
                </Grid>

                <UniformGrid Columns="5" Margin="0,0,-12,18">
                    <Border Style="{StaticResource StatCardStyle}">
                        <StackPanel>
                            <TextBlock Text="{Binding TotalItems}" FontSize="30" FontWeight="Bold"/>
                            <TextBlock Text="TOTAL" FontSize="11" Foreground="#6B7280" Margin="0,4,0,0"/>
                        </StackPanel>
                    </Border>
                    <Border Style="{StaticResource StatCardStyle}">
                        <StackPanel>
                            <TextBlock Text="{Binding OpenCount}" FontSize="30" FontWeight="Bold"
                                       Foreground="{StaticResource AccentBrush}"/>
                            <TextBlock Text="OPEN" FontSize="11" Foreground="#6B7280" Margin="0,4,0,0"/>
                        </StackPanel>
                    </Border>
                    <Border Style="{StaticResource StatCardStyle}">
                        <StackPanel>
                            <TextBlock Text="{Binding ImplementedCount}" FontSize="30" FontWeight="Bold"/>
                            <TextBlock Text="IMPLEMENTED" FontSize="11" Foreground="#6B7280" Margin="0,4,0,0"/>
                        </StackPanel>
                    </Border>
                    <Border Style="{StaticResource StatCardStyle}">
                        <StackPanel>
                            <TextBlock Text="{Binding VerifiedCount}" FontSize="30" FontWeight="Bold"
                                       Foreground="#D97706"/>
                            <TextBlock Text="VERIFIED" FontSize="11" Foreground="#6B7280" Margin="0,4,0,0"/>
                        </StackPanel>
                    </Border>
                    <Border Style="{StaticResource StatCardStyle}">
                        <StackPanel>
                            <TextBlock Text="{Binding ClosedCount}" FontSize="30" FontWeight="Bold"
                                       Foreground="{StaticResource AccentBrush}"/>
                            <TextBlock Text="CLOSED" FontSize="11" Foreground="#6B7280" Margin="0,4,0,0"/>
                        </StackPanel>
                    </Border>
                </UniformGrid>

                <TextBox Padding="8,6" Margin="0,0,0,18"
                         Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                         ToolTip="Search projects by name"/>

                <ItemsControl ItemsSource="{Binding ProjectSummariesView}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Style="{StaticResource ProjectCardStyle}">
                                <Button Style="{StaticResource CardClickButtonStyle}"
                                        ui:AutomationProperties.Name="{Binding ProjectName, StringFormat={}Open project {0}}"
                                        Command="{Binding DataContext.OpenProjectCommand,
                                                  ElementName=Root}"
                                        CommandParameter="{Binding}">
                                    <StackPanel>
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="Auto"/>
                                            </Grid.ColumnDefinitions>
                                            <TextBlock Grid.Column="0" Text="{Binding ProjectName}"
                                                       FontSize="16" FontWeight="SemiBold"/>
                                            <TextBlock Grid.Column="1" Text="{Binding StatusLabel}" FontSize="12"
                                                       FontWeight="SemiBold" Foreground="{StaticResource AccentBrush}"/>
                                        </Grid>
                                        <TextBlock FontSize="13" Foreground="#6B7280" Margin="0,6,0,0">
                                            <Run Text="{Binding ItemCount, Mode=OneWay}"/>
                                            <Run Text=" CAPAR(s)"/>
                                        </TextBlock>
                                    </StackPanel>
                                </Button>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>

                <TextBlock Text="No CAPARs yet. Open a project to add the first one."
                           Foreground="#6B7280" Margin="0,10,0,0">
                    <TextBlock.Style>
                        <Style TargetType="TextBlock">
                            <Setter Property="Visibility" Value="Collapsed"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding TotalItems}" Value="0">
                                    <Setter Property="Visibility" Value="Visible"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </TextBlock.Style>
                </TextBlock>

                <TextBlock Text="No projects match your search." Foreground="#6B7280" Margin="0,10,0,0"
                           Visibility="{Binding HasNoProjectSearchResults, Converter={StaticResource BoolToVisConverter}}"/>
            </StackPanel>

            <!-- ============ PROJECT CAPARS VIEW ============ -->
            <StackPanel Visibility="{Binding IsProjectItemsView, Converter={StaticResource BoolToVisConverter}}">

                <Button Content="&#8592; Back to Projects" ui:AutomationProperties.Name="Back to Projects" Command="{Binding BackToProjectsCommand}"
                        HorizontalAlignment="Left" Background="Transparent" BorderThickness="0"
                        Foreground="{StaticResource AccentBrush}" Cursor="Hand" Padding="0"
                        Margin="0,0,0,18"/>

                <Grid Margin="0,0,0,18">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <StackPanel Grid.Column="0">
                        <TextBlock Text="{Binding SelectedProjectDisplayName}" FontSize="24" FontWeight="Bold"
                                   TextWrapping="Wrap"/>
                        <TextBlock Text="{Binding SelectedProjectItemCount, StringFormat={}{0} CAPAR(s)}"
                                   FontSize="12" FontWeight="SemiBold" Foreground="#6B7280" Margin="0,3,0,0"/>
                    </StackPanel>
                    <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center" Margin="16,0,0,0">
                        <Button Content="&#8635; Refresh" Style="{StaticResource SecondaryButtonStyle}"
                                Command="{Binding RefreshCommand}" Margin="0,0,8,0"/>
                        <Button Content="+ New CAPAR" ui:AutomationProperties.Name="New CAPAR" Style="{StaticResource PrimaryButtonStyle}"
                                Command="{Binding ShowCreateFormCommand}"/>
                    </StackPanel>
                </Grid>

                <TextBox Padding="8,6" Margin="0,0,0,18"
                         Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                         ToolTip="Search by CAPAR number, title, project, owner, source, or root cause"/>

                <ItemsControl ItemsSource="{Binding ItemsView}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Style="{StaticResource CaparCardStyle}">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>

                                    <Button Grid.Column="0" Style="{StaticResource CardClickButtonStyle}"
                                        ui:AutomationProperties.Name="{Binding CaparNumber, StringFormat={}Open {0}}"
                                        Command="{Binding DataContext.OpenItemCommand,
                                                  ElementName=Root}"
                                        CommandParameter="{Binding}">
                                    <StackPanel>
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="Auto"/>
                                            </Grid.ColumnDefinitions>
                                            <TextBlock Grid.Column="0" Text="{Binding CaparNumber}"
                                                       FontSize="15" FontWeight="SemiBold"/>
                                            <TextBlock Grid.Column="1" Text="{Binding Status}" FontSize="12"
                                                       FontWeight="SemiBold" Foreground="{StaticResource AccentBrush}"/>
                                        </Grid>
                                        <TextBlock FontSize="12" Foreground="#6B7280" Margin="0,3,0,0"
                                                   TextWrapping="Wrap" MaxWidth="560"
                                                   Text="{Binding Title, TargetNullValue='Untitled'}"/>
                                        <TextBlock FontSize="11" Foreground="#9CA3AF" Margin="0,4,0,0">
                                            <Run Text="{Binding Source, Mode=OneWay}"/>
                                            <Run Text="• "/>
                                            <Run Text="{Binding Severity, Mode=OneWay}"/>
                                            <Run Text="• "/>
                                            <Run Text="{Binding Project, Mode=OneWay, TargetNullValue='Unassigned'}"/>
                                            <Run Text="• "/>
                                            <Run Text="{Binding Owner, Mode=OneWay, TargetNullValue='No owner'}"/>
                                        </TextBlock>
                                    </StackPanel>
                                </Button>

                                    <StackPanel Grid.Column="1" Orientation="Horizontal"
                                                VerticalAlignment="Center" Margin="16,0,0,0">
                                        <Button Content="Update" Style="{StaticResource SecondaryButtonStyle}"
                                                Margin="0,0,8,0"
                                                ui:AutomationProperties.Name="{Binding CaparNumber, StringFormat={}Update {0}}"
                                                Command="{Binding DataContext.EditItemCommand,
                                                          ElementName=Root}"
                                                CommandParameter="{Binding}"/>
                                        <Button Content="Delete" Style="{StaticResource DangerButtonStyle}"
                                                ui:AutomationProperties.Name="{Binding CaparNumber, StringFormat={}Delete {0}}"
                                                Command="{Binding DataContext.DeleteItemCommand,
                                                          ElementName=Root}"
                                                CommandParameter="{Binding}"/>
                                    </StackPanel>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>

                <TextBlock Text="No CAPARs for this project yet. Click + New CAPAR to add the first one."
                           Foreground="#6B7280" Margin="0,10,0,0">
                    <TextBlock.Style>
                        <Style TargetType="TextBlock">
                            <Setter Property="Visibility" Value="Collapsed"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding SelectedProjectItemCount}" Value="0">
                                    <Setter Property="Visibility" Value="Visible"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </TextBlock.Style>
                </TextBlock>

                <TextBlock Text="No CAPARs match your search." Foreground="#6B7280" Margin="0,10,0,0"
                           Visibility="{Binding HasNoSearchResults, Converter={StaticResource BoolToVisConverter}}"/>
            </StackPanel>

            <!-- ============ READ-ONLY DETAIL VIEW ============ -->
            <StackPanel Visibility="{Binding IsReadOnlyView, Converter={StaticResource BoolToVisConverter}}">

                <Button Content="&#8592; Back to CAPARs" ui:AutomationProperties.Name="Back to CAPARs" Command="{Binding BackToListCommand}"
                        HorizontalAlignment="Left" Background="Transparent" BorderThickness="0"
                        Foreground="{StaticResource AccentBrush}" Cursor="Hand" Padding="0"
                        Margin="0,0,0,18"/>

                <Border Style="{StaticResource StatCardStyle}">
                    <StackPanel>
                        <Grid Margin="0,0,0,4">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0" Text="{Binding Selected.CaparNumber}"
                                       FontSize="22" FontWeight="Bold" TextWrapping="Wrap"/>
                            <TextBlock Grid.Column="1" Text="{Binding Selected.Status}"
                                       FontSize="12" FontWeight="SemiBold"
                                       Foreground="{StaticResource AccentBrush}" VerticalAlignment="Top"/>
                        </Grid>

                        <TextBlock Text="{Binding Selected.Title}" FontSize="14" FontWeight="SemiBold"
                                   Foreground="#111827" TextWrapping="Wrap" Margin="0,2,0,4"/>

                        <TextBlock FontSize="11" Foreground="#9CA3AF" Margin="0,0,0,18">
                            <Run Text="{Binding Selected.Source, Mode=OneWay}"/>
                            <Run Text="• Last updated "/>
                            <Run Text="{Binding Selected.UpdatedAt, Mode=OneWay, StringFormat={}{0:MMM d, yyyy h:mm tt}}"/>
                        </TextBlock>

                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <StackPanel Grid.Row="0" Grid.Column="0" Margin="0,0,0,14">
                                <TextBlock Text="SEVERITY" Style="{StaticResource DetailLabelStyle}"/>
                                <TextBlock Text="{Binding Selected.Severity}" FontSize="14" Margin="0,3,0,0"/>
                            </StackPanel>
                            <StackPanel Grid.Row="0" Grid.Column="1" Margin="0,0,0,14">
                                <TextBlock Text="PROJECT" Style="{StaticResource DetailLabelStyle}"/>
                                <TextBlock Text="{Binding Selected.Project, TargetNullValue='Unassigned'}"
                                           FontSize="14" Margin="0,3,0,0"/>
                            </StackPanel>
                            <StackPanel Grid.Row="0" Grid.Column="2" Margin="0,0,0,14">
                                <TextBlock Text="OWNER" Style="{StaticResource DetailLabelStyle}"/>
                                <TextBlock Text="{Binding Selected.Owner, TargetNullValue='Not assigned'}"
                                           FontSize="14" Margin="0,3,0,0"/>
                            </StackPanel>
                            <StackPanel Grid.Row="1" Grid.Column="0" Margin="0,0,0,14">
                                <TextBlock Text="OPENED" Style="{StaticResource DetailLabelStyle}"/>
                                <TextBlock Text="{Binding Selected.OpenedDate, StringFormat=d}"
                                           FontSize="14" Margin="0,3,0,0"/>
                            </StackPanel>
                            <StackPanel Grid.Row="1" Grid.Column="1" Margin="0,0,0,14">
                                <TextBlock Text="DUE" Style="{StaticResource DetailLabelStyle}"/>
                                <TextBlock Text="{Binding Selected.DueDate, StringFormat=d, TargetNullValue='—'}"
                                           FontSize="14" Margin="0,3,0,0"/>
                            </StackPanel>
                            <StackPanel Grid.Row="1" Grid.Column="2" Margin="0,0,0,14">
                                <TextBlock Text="CLOSED" Style="{StaticResource DetailLabelStyle}"/>
                                <TextBlock Text="{Binding Selected.ClosedDate, StringFormat=d, TargetNullValue='—'}"
                                           FontSize="14" Margin="0,3,0,0"/>
                            </StackPanel>
                        </Grid>

                        <TextBlock Text="Root Cause" Style="{StaticResource DetailLabelStyle}"/>
                        <TextBlock Text="{Binding Selected.RootCause, TargetNullValue='None recorded.'}"
                                   TextWrapping="Wrap" FontSize="13" Foreground="#6B7280" Margin="0,3,0,14"/>

                        <TextBlock Text="Corrective Action" Style="{StaticResource DetailLabelStyle}"/>
                        <TextBlock Text="{Binding Selected.CorrectiveAction, TargetNullValue='None recorded.'}"
                                   TextWrapping="Wrap" FontSize="13" Foreground="#6B7280" Margin="0,3,0,14"/>

                        <TextBlock Text="Preventive Action" Style="{StaticResource DetailLabelStyle}"/>
                        <TextBlock Text="{Binding Selected.PreventiveAction, TargetNullValue='None recorded.'}"
                                   TextWrapping="Wrap" FontSize="13" Foreground="#6B7280" Margin="0,3,0,14"/>

                        <TextBlock Text="Remarks" Style="{StaticResource DetailLabelStyle}"/>
                        <TextBlock Text="{Binding Selected.Remarks, TargetNullValue='No remarks.'}"
                                   TextWrapping="Wrap" FontSize="13" Foreground="#6B7280" Margin="0,3,0,18"/>

                        <StackPanel Orientation="Horizontal">
<Button Content="Update" ui:AutomationProperties.Name="Update Selected" Style="{StaticResource PrimaryButtonStyle}"
                                        Command="{Binding EditItemCommand}"
                                        CommandParameter="{Binding Selected}" Margin="0,0,10,0"/>
<Button Content="Delete CAPAR" ui:AutomationProperties.Name="Delete Selected" Style="{StaticResource DangerButtonStyle}"
                                        Command="{Binding DeleteItemCommand}"
                                        CommandParameter="{Binding Selected}"/>
                        </StackPanel>
                    </StackPanel>
                </Border>
            </StackPanel>

            <!-- ============ FORM VIEW (shared: Create new + Update existing) ============ -->
            <StackPanel Visibility="{Binding IsFormView, Converter={StaticResource BoolToVisConverter}}">

                <TextBlock Text="{Binding FormTitle}" FontSize="22" FontWeight="Bold" Margin="0,0,0,18"/>

                <Border Style="{StaticResource StatCardStyle}">
                    <StackPanel>
                        <Grid Margin="0,0,0,14">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="10"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <StackPanel Grid.Column="0">
                                <TextBlock Text="CAPAR Number" Style="{StaticResource FieldLabelStyle}"/>
                                <TextBox Padding="8,6"
                                         Text="{Binding DraftCaparNumber, UpdateSourceTrigger=PropertyChanged}"/>
                            </StackPanel>
                            <StackPanel Grid.Column="2">
                                <TextBlock Text="Status" Style="{StaticResource FieldLabelStyle}"/>
                                <ComboBox Padding="8,6"
                                          ItemsSource="{Binding Statuses}"
                                          SelectedItem="{Binding DraftStatus}"/>
                            </StackPanel>
                        </Grid>

                        <TextBlock Text="Title" Style="{StaticResource FieldLabelStyle}"/>
                        <TextBox Padding="8,6" Margin="0,0,0,14" ui:AutomationProperties.Name="FormTitle"
                                 Text="{Binding DraftTitle, UpdateSourceTrigger=PropertyChanged}"/>

                        <Grid Margin="0,0,0,14">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="10"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <StackPanel Grid.Column="0">
                                <TextBlock Text="Source" Style="{StaticResource FieldLabelStyle}"/>
                                <ComboBox Padding="8,6"
                                          ItemsSource="{Binding Sources}"
                                          SelectedItem="{Binding DraftSource}"/>
                            </StackPanel>
                            <StackPanel Grid.Column="2">
                                <TextBlock Text="Severity" Style="{StaticResource FieldLabelStyle}"/>
                                <ComboBox Padding="8,6"
                                          ItemsSource="{Binding Severities}"
                                          SelectedItem="{Binding DraftSeverity}"/>
                            </StackPanel>
                        </Grid>

                        <Grid Margin="0,0,0,14">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="10"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <StackPanel Grid.Column="0">
                                <TextBlock Text="Project" Style="{StaticResource FieldLabelStyle}"/>
                                <ComboBox Padding="8,6"
                                          ItemsSource="{Binding Projects}"
                                          SelectedItem="{Binding DraftProject}"
                                          IsEditable="True"
                                          IsEnabled="{Binding CanChangeProject}"/>
                                <TextBlock Text="Filed under this project" FontSize="11"
                                           Foreground="#6B7280" Margin="0,4,0,0"
                                           Visibility="{Binding IsProjectLocked, Converter={StaticResource BoolToVisConverter}}"/>
                            </StackPanel>
                            <StackPanel Grid.Column="2">
                                <TextBlock Text="Owner" Style="{StaticResource FieldLabelStyle}"/>
                                <TextBox Padding="8,6"
                                         Text="{Binding DraftOwner, UpdateSourceTrigger=PropertyChanged}"/>
                            </StackPanel>
                        </Grid>

                        <Grid Margin="0,0,0,14">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="10"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="10"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <StackPanel Grid.Column="0">
                                <TextBlock Text="Opened" Style="{StaticResource FieldLabelStyle}"/>
                                <DatePicker Padding="8,6"
                                            SelectedDate="{Binding DraftOpenedDate}"/>
                            </StackPanel>
                            <StackPanel Grid.Column="2">
                                <TextBlock Text="Due" Style="{StaticResource FieldLabelStyle}"/>
                                <DatePicker Padding="8,6"
                                            SelectedDate="{Binding DraftDueDate}"/>
                            </StackPanel>
                            <StackPanel Grid.Column="4">
                                <TextBlock Text="Closed" Style="{StaticResource FieldLabelStyle}"/>
                                <DatePicker Padding="8,6"
                                            SelectedDate="{Binding DraftClosedDate}"/>
                            </StackPanel>
                        </Grid>

                        <TextBlock Text="Root Cause" Style="{StaticResource FieldLabelStyle}"/>
                        <TextBox Padding="10,8" MinHeight="60" AcceptsReturn="True" TextWrapping="Wrap"
                                 VerticalScrollBarVisibility="Auto" Margin="0,0,0,14"
                                 Text="{Binding DraftRootCause, UpdateSourceTrigger=PropertyChanged}"/>

                        <TextBlock Text="Corrective Action" Style="{StaticResource FieldLabelStyle}"/>
                        <TextBox Padding="10,8" MinHeight="60" AcceptsReturn="True" TextWrapping="Wrap"
                                 VerticalScrollBarVisibility="Auto" Margin="0,0,0,14"
                                 Text="{Binding DraftCorrectiveAction, UpdateSourceTrigger=PropertyChanged}"/>

                        <TextBlock Text="Preventive Action" Style="{StaticResource FieldLabelStyle}"/>
                        <TextBox Padding="10,8" MinHeight="60" AcceptsReturn="True" TextWrapping="Wrap"
                                 VerticalScrollBarVisibility="Auto" Margin="0,0,0,14"
                                 Text="{Binding DraftPreventiveAction, UpdateSourceTrigger=PropertyChanged}"/>

                        <TextBlock Text="Remarks" Style="{StaticResource FieldLabelStyle}"/>
                        <TextBox Padding="10,8" MinHeight="60" AcceptsReturn="True" TextWrapping="Wrap"
                                 VerticalScrollBarVisibility="Auto" Margin="0,0,0,18"
                                 Text="{Binding DraftRemarks, UpdateSourceTrigger=PropertyChanged}"/>

                        <StackPanel Orientation="Horizontal">
                            <Button Content="{Binding SaveButtonLabel}" Style="{StaticResource PrimaryButtonStyle}"
                                    Command="{Binding SaveFormCommand}" Margin="0,0,10,0"/>
                            <Button Content="Cancel" Style="{StaticResource SecondaryButtonStyle}"
                                    Command="{Binding CancelFormCommand}"/>
                        </StackPanel>
                    </StackPanel>
                </Border>
            </StackPanel>

        </Grid>
    </ScrollViewer>
</UserControl>